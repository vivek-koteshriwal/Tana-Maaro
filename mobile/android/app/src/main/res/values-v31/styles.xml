<?xml version="1.0" encoding="utf-8"?>
<!--
    Android 12+ (API 31+) splash screen using the native SplashScreen API.

    windowSplashScreenBackground      → full-screen black fill
    windowSplashScreenAnimatedIcon    → Tana Maaro logo, inset to fit the 288 dp
                                        icon circle
    windowSplashScreenIconBackgroundColor → black circle behind the icon so no
                                        white/grey default shows through
    postSplashScreenTheme             → switches to NormalTheme once Flutter's
                                        first frame is drawn (seamless cut)
-->
<resources>

    <style name="LaunchTheme" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/splash_icon</item>
        <item name="windowSplashScreenIconBackgroundColor">@color/splash_icon_background</item>
        <item name="postSplashScreenTheme">@style/NormalTheme</item>
    </style>

    <style name="NormalTheme" parent="@android:style/Theme.Black.NoTitleBar">
        <item name="android:windowBackground">@color/splash_background</item>
    </style>

</resources>
